// Package engruntime provides optional runtime primitives that compose with the
// deterministic engine core.
//
// The package is intentionally domain-agnostic and includes:
// - scheduler abstraction for delayed/background execution
// - typed topic-based event streams with subscription filtering
package engruntime
